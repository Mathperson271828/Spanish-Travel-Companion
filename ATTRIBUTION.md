***

# Project Attributions

## Gemini Generated Code

## Format of Each Attribution
## Number
## Date
## The Tool
## The Description
## Actual Code

## 1
## April 22, 2026
## Gemini 3 Fast
## Created the general framework for the backend of the project. We load the environment, initialize the app object, allow for the frontend to send requests to the backend, define exactly what the data the frontend sends to the backend should look like, a health-check tool to see if the code is actually working, and a rough setup for the main intersection for the website.

```python
import os
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from dotenv import load_dotenv
from pydantic import BaseModel

## The code below was generated by Gemini (#1)
# 1. ENVIRONMENT LOADING
# This function looks for your .env file and reads the API keys into memory.
# It allows us to use os.getenv() later without hardcoding secrets in the code.
load_dotenv()

# 2. APP INITIALIZATION
# Here we create the "app" object, which is the heart of your server.
app = FastAPI(title="Spanish Travel Companion API")

# 3. CORS (CROSS-ORIGIN RESOURCE SHARING) CONFIGURATION
# By default, browsers block websites from talking to different servers.
# This block tells the browser: "It is safe for my Next.js frontend (on port 3000)
# to send requests to this FastAPI backend (on port 8000)."
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"], # Permits requests from your frontend
    allow_credentials=True,                  # Allows cookies/auth headers if needed
    allow_methods=["*"],                     # Permits all actions (GET, POST, etc.)
    allow_headers=["*"],                     # Permits all metadata headers
)

# 4. DATA MODELING
# We use Pydantic to define exactly what the incoming data should look like.
# This tells FastAPI: "If someone sends a POST request to /chat, it MUST 
# contain a JSON object with a string field named 'message'."
class ChatRequest(BaseModel):
    message: str

# 5. HEALTH CHECK ENDPOINT (GET)
# This is a simple diagnostic tool. You can visit http://localhost:8000/health
# in your browser to verify the server is actually running.
@app.get("/health")
async def health_check():
    return {
        "status": "online",
        "message": "¡Hola Leo! The backend server is up and running."
    }

# 6. CHAT ENDPOINT (POST)
# This is where the main interaction happens. We use 'POST' because the 
# frontend is "posting" new data (the user's message) to the server.
@app.post("/chat")
async def chat_endpoint(request: ChatRequest):
    """
    Temporary endpoint that receives a message and returns a Spanish response.
    In the next step, this is where Whisper and ChatGPT will be integrated.
    """
    # 'request.message' pulls the actual text sent from the frontend
    user_text = request.message
    
    # For now, we return a hardcoded response to verify the "Handshake" works.
    return {
        "user_sent": user_text,
        "reply": "Hola, soy tu compañero de viaje. ¿En qué puedo ayudarte hoy?",
        "difficulty": "Beginner"
    }
```

## 2
## April 22, 2026
## Gemini 3 Fast
## Created a rough sketch of the frontend mechanism. This acts as a rough UI and connection to backend. This includes the react essentials, the states or memory, the messenger or send message function with a rough request and response outline, and the face or the UI.

```tsx
/** The code below was generated by Gemini (#2) **/
"use client"; // Directs Next.js to render this on the client side (the browser) to allow for user interaction and state.

import { useState } from "react"; // Hook to manage data that changes over time (like user input and AI responses).

export default function Home() {
  /**
   * STATE MANAGEMENT:
   * 'input' stores what the user types in the text box.
   * 'reply' stores the final response string received from the FastAPI backend.
   * 'loading' is a boolean flag to track if a request is currently in flight (used to show "Sending..." on the button).
   */
  const [input, setInput] = useState("");
  const [reply, setReply] = useState("");
  const [loading, setLoading] = useState(false);

  /**
   * ASYNCHRONOUS COMMUNICATION FUNCTION:
   * This handles the "Handshake" with your Python server. 
   * It is 'async' because we don't want the UI to freeze while waiting for a response over the network.
   */
  const sendMessage = async () => {
    // Step 1: Start loading state to disable the button and give user feedback
    setLoading(true);
    
    try {
      /**
       * FETCH API CALL:
       * We send a POST request to our local FastAPI server.
       * - method: "POST" because we are sending data, not just requesting it.
       * - headers: Informs the server that the body contains JSON data.
       * - body: Converts the JavaScript object { message: input } into a JSON string.
       */
      const response = await fetch("https://spanish-travel-companion.onrender.com/chat", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({ message: input }),
      });

      // Step 2: Extract the JSON body from the server's response
      const data = await response.json();
      
      /**
       * Step 3: Update the 'reply' state with the message from the backend.
       * Based on our main_backend.py, the backend returns: {"reply": "...", "difficulty": "..."}
       */
      setReply(data.reply);
      
    } catch (error) {
      // Error Handling: If the backend is offline or the network fails, log the error and notify the user.
      console.error("Error communicating with backend:", error);
      setReply("Error: Could not reach the server. Make sure your FastAPI backend is running.");
    } finally {
      // Step 4: Turn off loading state regardless of whether the request succeeded or failed.
      setLoading(false);
    }
  };

  return (
    // MAIN LAYOUT CONTAINER
    <main style={{ padding: "50px", textAlign: "center", fontFamily: "sans-serif" }}>
      <h1>Spanish Travel Companion</h1>
      <p>Test the connection to your FastAPI backend below:</p>
      
      {/* TEXT INPUT:
          - value={input}: This is a "controlled component," meaning the box always shows what's in our state.
          - onChange: Updates the state every time a key is pressed.
      */}
      <input 
        type="text" 
        value={input}
        onChange={(e) => setInput(e.target.value)}
        placeholder="Type something to test..."
        style={{ 
          padding: "10px", 
          borderRadius: "5px", 
          border: "1px solid #ccc",
          color: "black",
          width: "300px"
        }}
      />
      
      {/* ACTION BUTTON:
          - onClick: Triggers our sendMessage function.
          - disabled: Prevents multiple clicks while a request is loading.
      */}
      <button 
        onClick={sendMessage} 
        disabled={loading}
        style={{ 
          marginLeft: "10px", 
          padding: "10px 20px", 
          cursor: loading ? "not-allowed" : "pointer",
          backgroundColor: "#0070f3",
          color: "white",
          border: "none",
          borderRadius: "5px"
        }}
      >
        {loading ? "Sending..." : "Send Message"}
      </button>

      {/* CONDITIONAL RENDERING:
          This block only appears on the screen once 'reply' has a value.
          The '&&' operator acts as a gatekeeper.
      */}
      {reply && (
        <div style={{ 
          marginTop: "30px", 
          padding: "20px", 
          border: "1px solid #0070f3", 
          borderRadius: "10px",
          display: "inline-block" 
        }}>
          <h2 style={{ fontSize: "1rem", color: "#555" }}>Backend Response:</h2>
          <p style={{ fontSize: "1.2rem", fontWeight: "bold" }}>{reply}</p>
        </div>
      )}
    </main>
  );
}
```

## 3
## April 22, 2026
## Gemini 3 Fast
## Edited the backend, main_backend.py file of the backend folder in order to help the user be able to talk to an actual chatbot. Essentially we added API keys to chatGPT for the respoonses to the user. Below is the changed section.

```python
# 6. CHAT ENDPOINT (POST)
# This is where the main interaction happens. We use 'POST' because the 
# frontend is "posting" new data (the user's message) to the server.
@app.post("/chat")
async def chat_endpoint(request: ChatRequest):
    """
    Temporary endpoint that receives a message and returns a Spanish response.
    In the next step, this is where Whisper and ChatGPT will be integrated.
    """
    # 'request.message' pulls the actual text sent from the frontend
    user_text = request.message

    try:
        # Call the GPT API
        response = client.chat.completions.create (model = "gpt-4o-mini", 
                                                  messages = [
                                                      {"role": "system", "content": "You are a helpful Spanish travel companion. Respond in simple Spanish suitable for a beginner learner."},
                                                      {"role": "user", "content" : request.message}
                                                  ])
        
        ai_reply = response.choices[0].message.content

        return {
            "user_sent": user_text,
            "reply": ai_reply,
            "difficulty": "Beginner"
        }

    except Exception as e:
        print(f"Error: {e}")
        return{"reply": "Lo siento, I had trouble thinking. Check your API key!", 
               "status": "error"
        }
```

## 4
## April 24, 2026
## Gemini 3 Fast
## Modified sections of code with the assistance of Gemini such as the main_backend.py file and the page.tsx file. The main added feature was a button to toggle the difficulty of the responses of the bot.

```python
@app.post("/chat")
async def chat_endpoint(request: ChatRequest):
    #Define out the teaching styles
    user_text = request.message

    if request.difficulty == "Beginner":
        level_instruction = (
            "Use only A1-level Spanish. Use present tense only."
            "Keep sentences under 8 words and avoid slang."
        )
    else:
        level_instruction = (
            "Use A2 and B1 level Spanish. You can use past and future tenses."
            "Use slightly more complex travel vocabulary and longer sentences."
        )

    try:
        response = client.chat.completions.create(
            model = "gpt-4o-mini",
            messages = [
                {
                    "role" : "system",
                    "content" : f"You are a Spanish travel companion. {level_instruction} Respond in Spanish."
                },
                {"role" : "user", "content" : user_text}
            ]
        )
    
        ai_reply = response.choices[0].message.content

        if request.difficulty == "Beginner":
            bot_difficulty = "Beginner"
        else:
            bot_difficulty = "Intermediate"            

        return {
            "user_sent": user_text,
            "reply": ai_reply,
            "difficulty" : bot_difficulty
        }

    except Exception as e:
        print(f"Error: {e}")
        return{
            "reply": "Lo siento, I had trouble thinking. Check your API key!", 
            "status": "error"
        }

        <div style = {{ marginBottom: "20px"}}>
        <button
          onClick={() => setDifficulty("Beginner")}
          style={{
            backgroundColor: difficulty === "Beginner" ? "#0070f3" : "#ccc",
            color: "white", padding: "10px", marginRight: "10px", borderRadius: "5px"
          }}
        >
          Beginner (Simple)
        </button>
        <button 
        onClick={() => setDifficulty("Intermediate")}
        style={{ 
          backgroundColor: difficulty === "Intermediate" ? "#0070f3" : "#ccc",
          color: "white", padding: "10px", borderRadius: "5px"
        }}
      >
        Intermediate (Natural)
      </button>
      </div>
    </main>
```

## 5
## April 24, 2026
## Gemini 3 Fast
## Asked Gemini to generate me code for the frontend to send sound files to the backend and code for the backend to process audio files properly using Whisper.

```typescript
// Inside your Home component in page.tsx
const [recording, setRecording] = useState(false);
const [mediaRecorder, setMediaRecorder] = useState<MediaRecorder | null>(null);

const startRecording = async () => {
  const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
  const recorder = new MediaRecorder(stream);
  let chunks: BlobPart[] = [];

  recorder.ondataavailable = (e) => chunks.push(e.data);
  recorder.onstop = async () => {
    const audioBlob = new Blob(chunks, { type: 'audio/webm' });
    sendAudioToBackend(audioBlob);
  };

  recorder.start();
  setMediaRecorder(recorder);
  setRecording(true);
};

const stopRecording = () => {
  mediaRecorder?.stop();
  setRecording(false);
};

from fastapi import UploadFile, File

@app.post("/speech-to-text")
async def transcribe_speech(file: UploadFile = File(...)):
    # 1. Save the uploaded file temporarily
    with open("temp_audio.webm", "wb") as buffer:
        buffer.write(await file.read())

    # 2. Open the file and send it to Whisper
    audio_file = open("temp_audio.webm", "rb")
    transcript = client.audio.transcriptions.create(
        model="whisper-1", 
        file=audio_file
    )

    # 3. Return the text to the frontend
    return {"text": transcript.text}
```

## 6
## April 24, 2026
## Gemini 3 Fast
## Asked Gemini to generate me Code to handle the sendAudioToBackend error as that function does not exist so I asked it to build that function for me and generate me a button to send audio.

```tsx
const sendAudioToBackend = async (blob: Blob) => {
  setLoading(true);
  try {
    // 1. Create a "Form" container to hold the audio file
    const formData = new FormData();
    // We name the field "file" to match what FastAPI expects
    formData.append("file", blob, "recording.webm");

    // 2. Send it to your NEW speech-to-text endpoint
    const response = await fetch("https://spanish-travel-companion.onrender.com/speech-to-text", {
      method: "POST",
      body: formData, // No headers needed! The browser handles 'multipart/form-data' automatically.
    });

    const data = await response.json();

    // 3. Take the text Whisper found and put it in the input box
    if (data.text) {
      setInput(data.text);
      // Optional: Automatically trigger the chat response
      // sendMessage(data.text); 
    }
  } catch (error) {
    console.error("Speech-to-text failed:", error);
  } finally {
    setLoading(false);
  }
};

<div style={{ marginTop: "20px" }}>
  {!recording ? (
    <button onClick={startRecording} style={{ padding: "10px", backgroundColor: "green", color: "white" }}>
      🎤 Start Recording
    </button>
  ) : (
    <button onClick={stopRecording} style={{ padding: "10px", backgroundColor: "red", color: "white" }}>
      🛑 Stop & Transcribe
    </button>
  )}
</div>
```

## 7 
## April 24, 2026
## Gemini 3 Fast
## Used Gemini 3 to help me debug some code and issues in the original code it gave me.

```tsx
// It should look exactly like this inside your export default function Home() {
const [input, setInput] = useState("");
const data = await response.json() as { text?: string }; // Tell TS it might have a "text" key

if (data.text) {
  setInput(data.text);
}
export default function Home() {
  // 1. THIS IS WHERE setInput IS BORN
  const [input, setInput] = useState("");

  const sendAudioToBackend = async (blob: Blob) => {
    try {
      const formData = new FormData();
      formData.append("file", blob, "audio.webm");

      const response = await fetch("https://spanish-travel-companion.onrender.com/speech-to-text", {
        method: "POST",
        body: formData,
      });

      // 2. TELL TYPESCRIPT TO EXPECT A "text" KEY
      const data = await response.json() as { text?: string };

      if (data.text) {
        // 3. NOW YOU CAN CALL setInput SAFELY
        setInput(data.text);
      }
    } catch (error) {
      console.error(error);
    }
  };

  return (
    // Your UI goes here...
  );
}
if (data.text) {
  // The "|| ''" means: Use data.text, but if it's missing, use an empty string
  setInput(data.text || ""); 
}
```

## 8
## April 24, 2026
## Gemini 3 Fast
## Used Gemini 3 to help modify the frontend and backend files to account for the case that the user does not record any audio at all and sends an empty file to the backend.

```typescript
const sendAudioToBackend = async (blob: Blob) => {
  // GUARD: If the file is basically empty (under 1KB), stop here
  if (blob.size < 1000) { 
    console.warn("Audio file is too small or empty.");
    return; 
  }

  setLoading(true);
  try {
    // ... existing fetch logic
const sendAudioToBackend = async (blob: Blob) => {
  // GUARD: If the file is basically empty (under 1KB), stop here
  if (blob.size < 1000) { 
    console.warn("Audio file is too small or empty.");
    return; 
  }

  setLoading(true);
  try {
    // ... existing fetch logic
```

## 9
## April 24, 2026
## Gemini 3 Pro
## Used Gemini 3 to debug a lot of code. Now the user can successfully record their audio and the bot will respond in Spanish text.

```python
@app.post("/speech-to-text")
async def transcribe_speech(file: UploadFile = File(...)):
    content = await file.read()
    
    # GUARD 1: Empty file check
    if len(content) == 0:
        return {"text": "", "error": "No audio detected"}

    try:
        transcript = await client.audio.transcriptions.create(
            model="whisper-1",
            file=("audio.webm", content) 
        )
        
        # CRITICAL FIX: You must actually RETURN the text!
        return {"text": transcript.text}
        
    except Exception as e:
        print(f"Whisper API Error: {e}")
        # MUST RETURN an error dictionary if it fails
        return {"error": "Failed to transcribe audio"}

import io

transcript = await client.audio.transcriptions.create(
    model="whisper-1",
    file=("audio.webm", io.BytesIO(content)) # <-- Wraps the bytes in a fake file
)

# BAD: Python doesn't wait for OpenAI to finish
transcript = client.audio.transcriptions.create(
    model="whisper-1",
    file=("audio.webm", content) 
)

# GOOD: Python pauses here until the transcription is done
transcript = await client.audio.transcriptions.create(
    model="whisper-1",
    file=("audio.webm", content) 
)

# Make sure to import your ChatRequest model at the top of the file!

@app.post("/speech-to-text")
async def transcribe_speech(file: UploadFile = File(...)):
    content = await file.read()
    if len(content) == 0:
        return {"text": "", "error": "No audio detected"}

    try:
        # 1. Get the text from Whisper
        transcript = await client.audio.transcriptions.create(
            model="whisper-1",
            file=("audio.webm", io.BytesIO(content))
        )
        user_text = transcript.text

        # 2. Package it into the request your chat endpoint expects
        # (Change 'message' to whatever variable your ChatRequest uses)
        chat_request = ChatRequest(message=user_text)

        # 3. Call your chatbot directly! (Don't forget 'await')
        bot_response = await chat_endpoint(chat_request)

        # 4. Return BOTH to the frontend so it can update the UI
        return {
            "text": user_text,             # What the user said
            "bot_response": bot_response   # What the bot replied
        }

    except Exception as e:
        print(f"Whisper/Chat API Error: {e}")
        return {"error": "Failed to process voice message"}


// 5. If we safely made it here, set the text AND send the message!
if (data.text) {
  // Update the input box so the user sees what Whisper heard
  setInput(data.text); 
  
  // IMMEDIATELY trigger your existing chat function!
  // (Replace 'sendMessage' with whatever your actual chat function is named)
  await sendMessage(data.text); 
}

content = await file.read() # Read 1: Plays the tape, saves it to memory

with open("temp_audio.webm", "wb") as buffer:
    buffer.write(await file.read()) # Read 2: The tape is already at the end! Writes 0 bytes.

import io # Make sure this is at the top of your file!

@app.post("/speech-to-text")
async def transcribe_speech(file: UploadFile = File(...)):
    # 1. Read the audio into lightning-fast memory (RAM)
    content = await file.read()
    
    if len(content) == 0:
        return {"text": "", "error": "No audio detected"}

    try:
        # 2. Wrap the memory in a "fake file" (BytesIO) and send it directly to OpenAI
        transcript = await client.audio.transcriptions.create(
            model="whisper-1",
            file=("audio.webm", io.BytesIO(content))
        )
        
        # 3. Return the text back to React!
        return {"text": transcript.text}

    except Exception as e:
        print(f"Whisper API Error: {e}")
        return {"error": "Failed to transcribe audio"}

// We add an optional parameter: textOverride (the '?' makes it optional)
const sendMessage = async (textOverride?: string) => {
  // Logic check: Use the Whisper text if provided, otherwise use the typed input box state
  const messageToSend = textOverride || input;

  // Optional safety guard: don't send empty messages to the backend
  if (!messageToSend.trim()) return; 

  setLoading(true);

  try {
    const response = await fetch("https://spanish-travel-companion.onrender.com/chat", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
      },
      // Send the guaranteed string, not the React state!
      body: JSON.stringify({ message: messageToSend, difficulty: difficulty }), 
    });

    const data = await response.json();
    setReply(data.reply);
      
  } catch (error) {
    console.error("Error communicating with backend:", error);
    setReply("Error: Could not reach the server. Make sure your FastAPI backend is running.");
  } finally {
    setLoading(false);
  }
};

transcript = await client.audio.transcriptions.create(
            model="whisper-1",
            file=("audio.webm", io.BytesIO(content)),
            language="es" # <--- THIS IS THE MAGIC FIX
        )

<button onClick={sendMessage}> 
  Send 
</button>


<button onClick={() => sendMessage()}> 
  Send 
</button>


<form onSubmit={(e) => {
  e.preventDefault();
  sendMessage();
}}>

// ❌ BAD: The object is inside the div like text!
<div>
   {{ marginLeft: "10px" }} 
</div>

// ✅ GOOD: It must be attached to the 'style' prop inside the opening tag
<div style={{ marginLeft: "10px" }}>
</div>


// ❌ BAD: React doesn't know what the HTML attribute 'marginLeft' is
<div marginLeft="10px"> Hello </div>

// ❌ BAD: Missing the word 'style='
<div {{ marginLeft: "10px" }}> Hello </div>

// ✅ GOOD: Wrapped safely in the style tag with double curly braces
<div style={{ marginLeft: "10px" }}> Hello </div>

{/* ACTION BUTTON:
          - onClick: Triggers our sendMessage function.
          - disabled: Prevents multiple clicks while a request is loading.
      */}
      <button 
        onClick={() => sendMessage()}
        disabled={loading}
        style={{ 
          marginLeft: "10px", 
          padding: "10px 20px", 
          cursor: loading ? "not-allowed" : "pointer",
          backgroundColor: "#0070f3",
          color: "white",
          border: "none",
          borderRadius: "5px"
        }}
      >
        {loading ? "Sending..." : "Send Message"}
      </button>
```

## 10
## April 25, 2026
## Gemini 3 Pro
## Used Gemini to help me generate code to be able to add the speed button at which the bot talks back and enable the bot to be able to respond to what I am saying

```typescript
from fastapi.responses import StreamingResponse
from pydantic import BaseModel
from elevenlabs.client import ElevenLabs
import os

# Initialize ElevenLabs (it will automatically look for ELEVENLABS_API_KEY)
elevenlabs_client = ElevenLabs(api_key=os.getenv("ELEVENLABS_API_KEY"))

# Create a data model for the incoming text
class TTSRequest(BaseModel):
    text: str

@app.post("/text-to-speech")
async def generate_speech(request: TTSRequest):
    try:
        # Generate the audio stream
        # IMPORTANT: We use the multilingual model so the Spanish accent is perfect
        audio_stream = elevenlabs_client.generate(
            text=request.text,
            voice="Rachel", # You can swap this with a Spanish voice ID from your ElevenLabs dashboard
            model="eleven_multilingual_v2" 
        )
        
        # A generator function to stream the chunks back to Next.js
        def iterfile():
            for chunk in audio_stream:
                if chunk:
                    yield chunk

        # Return the audio as a playable MP3 stream
        return StreamingResponse(iterfile(), media_type="audio/mpeg")

    except Exception as e:
        print(f"ElevenLabs Error: {e}")
        return {"error": "Failed to generate audio"}

// 1.0 is Normal, 0.75 is Slow, 1.25 is Fast
const [audioSpeed, setAudioSpeed] = useState<number>(1.0);

const playAudioResponse = async (textToSpeak: string) => {
  try {
    const response = await fetch("https://spanish-travel-companion.onrender.com/text-to-speech", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ text: textToSpeak }),
    });

    if (!response.ok) throw new Error("Failed to fetch audio");

    // Convert the incoming stream into a playable "Blob"
    const audioBlob = await response.blob();
    const audioUrl = URL.createObjectURL(audioBlob);
    
    // Create an HTML5 Audio object
    const audio = new Audio(audioUrl);
    
    // THE SPEED TRICK: Set the playback speed dynamically
    audio.playbackRate = audioSpeed; 
    
    // Play it!
    audio.play();

  } catch (error) {
    console.error("Audio playback error:", error);
  }
};

const data = await response.json();
    setReply(data.reply);
    
    // Trigger ElevenLabs to speak the response!
    if (data.reply) {
       await playAudioResponse(data.reply);
    }

<div style={{ marginTop: "20px", display: "flex", gap: "10px", alignItems: "center" }}>
  <span>Playback Speed:</span>
  <button 
    onClick={() => setAudioSpeed(0.75)}
    style={{ fontWeight: audioSpeed === 0.75 ? "bold" : "normal" }}
  >
    Slow (0.75x)
  </button>
  
  <button 
    onClick={() => setAudioSpeed(1.0)}
    style={{ fontWeight: audioSpeed === 1.0 ? "bold" : "normal" }}
  >
    Normal (1x)
  </button>
  
  <button 
    onClick={() => setAudioSpeed(1.25)}
    style={{ fontWeight: audioSpeed === 1.25 ? "bold" : "normal" }}
  >
    Fast (1.25x)
  </button>
</div>
```

## 11
## April 25, 2026
## Gemini 3 Pro
## Used Gemini to help me debug a lot of code, so now the user can speak and can hear sound play back!!!

```python
from fastapi.responses import Response 
from fastapi import HTTPException      

@app.post("/text-to-speech")
async def generate_speech(request: TTSRequest):
    try:
        # The NEW v1.x syntax uses text_to_speech.convert
        audio_generator = elevenlabs_client.text_to_speech.convert(
            text=request.text,
            # You must use the exact Voice ID now, not the name! This is Rachel:
            voice_id="21m00Tcm4TlvDq8ikWAM", 
            model_id="eleven_multilingual_v2",
            output_format="mp3_44100_128"
        )
        
        # Glue the streaming chunks together into one complete MP3 file
        audio_bytes = b"".join(audio_generator)
        
        # Return the finished MP3 directly to Next.js
        return Response(content=audio_bytes, media_type="audio/mpeg")

    except Exception as e:
        print(f"ElevenLabs Error: {e}")
        # Force a 500 Server Error so the frontend knows it failed
        raise HTTPException(status_code=500, detail=str(e))

from pydantic import BaseModel

class TTSRequest(BaseModel):
    text: str

audio_generator = elevenlabs_client.text_to_speech.convert(
            text=request.text,
            voice="Rachel",  # <--- THIS IS THE CULPRIT!
            model_id="eleven_multilingual_v2",
            output_format="mp3_44100_128"
        )

audio_generator = elevenlabs_client.text_to_speech.convert(
            text=request.text,
            voice_id="21m00Tcm4TlvDq8ikWAM", # <--- MUST be 'voice_id' with the exact ID string
            model_id="eleven_multilingual_v2",
            output_format="mp3_44100_128"
        )

# Make sure you still have Response and HTTPException imported
from fastapi.responses import Response 
from fastapi import HTTPException      

@app.post("/text-to-speech")
async def generate_speech(request: TTSRequest):
    try:
        # We use your existing 'client' (OpenAI) instead of ElevenLabs!
        # "nova" is a great, energetic female voice. "alloy" is a good neutral voice.
        response = await client.audio.speech.create(
            model="tts-1",
            voice="nova", 
            input=request.text
        )
        
        # Read the raw audio bytes
        audio_bytes = response.read()
        
        # Return the MP3 straight to your Next.js frontend
        return Response(content=audio_bytes, media_type="audio/mpeg")

    except Exception as e:
        print(f"OpenAI TTS Error: {e}")
        raise HTTPException(status_code=500, detail=str(e))

# 1. MAKE SURE to import Response, NOT StreamingResponse
from fastapi import APIRouter, File, UploadFile, HTTPException
from fastapi.responses import Response 
import io

@app.post("/text-to-speech")
async def generate_speech(request: TTSRequest):
    try:
        # 2. Call OpenAI Text-to-Speech
        response = await client.audio.speech.create(
            model="tts-1",
            voice="nova", # 'nova' or 'alloy' are great choices
            input=request.text
        )
        
        # 3. Extract the raw bytes from the response
        audio_bytes = response.read()
        
        # 4. Return the MP3 directly using the standard Response!
        return Response(content=audio_bytes, media_type="audio/mpeg")

    except Exception as e:
        print(f"OpenAI TTS Error: {e}")
        raise HTTPException(status_code=500, detail=str(e))
```

## 12
## April 25, 2026
## Gemini 3 Pro
## Used Gemini to improve the sound quality of the output by using the integraed speed function in OpenAI instead of manually creating a new one to stretch out the audio

```typescript
# 1. Update the request model to accept a speed number
class TTSRequest(BaseModel):
    text: str
    speed: float = 1.0  # Default to normal speed if none is provided

@app.post("/text-to-speech")
async def generate_speech(request: TTSRequest):
    try:
        response = await client.audio.speech.create(
            model="tts-1",
            voice="nova", 
            input=request.text,
            speed=request.speed # 2. Pass the speed directly to OpenAI!
        )
        
        audio_bytes = response.read()
        return Response(content=audio_bytes, media_type="audio/mpeg")

    except Exception as e:
        print(f"OpenAI TTS Error: {e}")
        raise HTTPException(status_code=500, detail=str(e))
// Add this outside your component or at the top of the file to protect the audio
let currentAudio: HTMLAudioElement | null = null;

const playAudioResponse = async (textToSpeak: string) => {
  try {
    const response = await fetch("https://spanish-travel-companion.onrender.com/text-to-speech", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
      },
      // 1. Send the speed to the backend!
      body: JSON.stringify({ text: textToSpeak, speed: audioSpeed }),
    });

    if (!response.ok) throw new Error("Failed to fetch audio");

    const audioBlob = await response.blob();
    const audioUrl = URL.createObjectURL(audioBlob);
    
    // 2. Stop the old audio if the bot is already speaking
    if (currentAudio) {
      currentAudio.pause();
    }
    
    // 3. Save the new audio to our protected variable
    currentAudio = new Audio(audioUrl);
    
    // (Notice we DELETED the audio.playbackRate line here!)
    
    // 4. Play it safely
    currentAudio.play();

  } catch (error) {
    console.error("Audio playback error:", error);
  }
};
```


## 13
## April 25, 2026
## Gemini 3 Pro
## Used Gemini to generate code to allow the user to interact with the bot for multiple cycles without having to keep on clicking the microphone button. Also asked Gemini to help me generate code to create a hard stop that would turn the microphone off after 10 seconds of silence.

```tsx
import { useRef, useState } from "react"; // Make sure useRef is imported!

// Inside your component:
const silenceTimer = useRef<NodeJS.Timeout | null>(null);
const audioContext = useRef<AudioContext | null>(null);

const startRecording = async () => {
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
    
    // 1. Standard MediaRecorder setup (keep your existing setup here)
    const recorder = new MediaRecorder(stream);
    const chunks: BlobPart[] = [];
    recorder.ondataavailable = (e) => chunks.push(e.data);
    recorder.onstop = () => {
      const blob = new Blob(chunks, { type: "audio/webm" });
      sendAudioToBackend(blob); 
    };
    
    // Set your React state
    mediaRecorder.current = recorder;
    setRecording(true);
    recorder.start();

    // ==========================================
    // 2. THE SILENCE DETECTOR
    // ==========================================
    audioContext.current = new AudioContext();
    const source = audioContext.current.createMediaStreamSource(stream);
    const analyzer = audioContext.current.createAnalyser();
    analyzer.fftSize = 512;
    source.connect(analyzer);

    const bufferLength = analyzer.frequencyBinCount;
    const dataArray = new Uint8Array(bufferLength);

    // This function runs 60 times a second to check the volume
    const checkSilence = () => {
      if (recorder.state !== "recording") return;

      analyzer.getByteFrequencyData(dataArray);
      
      // Calculate the average volume right now
      const currentVolume = dataArray.reduce((a, b) => a + b, 0) / bufferLength;

      // VOLUME THRESHOLD: 10 is usually a good baseline. 
      // If it's above 10, someone is talking!
      if (currentVolume > 10) {
        // They are speaking! Cancel the silence timer.
        if (silenceTimer.current) {
          clearTimeout(silenceTimer.current);
          silenceTimer.current = null;
        }
      } else {
        // It is silent. If the timer isn't running yet, start it!
        if (!silenceTimer.current) {
          silenceTimer.current = setTimeout(() => {
            console.log("3 seconds of silence detected. Stopping!");
            recorder.stop(); // Automatically trigger Whisper!
          }, 3000); // 3000 milliseconds = 3 seconds
        }
      }

      // Loop this function to keep checking
      requestAnimationFrame(checkSilence);
    };

    // Kick off the loop
    checkSilence();

  } catch (error) {
    console.error("Error accessing microphone:", error);
  }
};

const stopRecording = () => {
  if (mediaRecorder.current && mediaRecorder.current.state === "recording") {
    mediaRecorder.current.stop();
    setRecording(false);
  }

  // Clear the silence timer
  if (silenceTimer.current) {
    clearTimeout(silenceTimer.current);
    silenceTimer.current = null;
  }

  // Turn off the volume analyzer
  if (audioContext.current) {
    audioContext.current.close();
    audioContext.current = null;
  }
};

} catch (error) {
        console.error("Error accessing microphone:", error);
      }
    }; // <-- This one closes the 'try/catch' block properly
  }; // <-- THIS IS THE ROGUE BRACKET!

  const startRecording = async () => {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
      const recorder = new MediaRecorder(stream);
      let chunks: BlobPart[] = [];

      recorder.ondataavailable = (e) => chunks.push(e.data);
      recorder.onstop = async () => {
        const audioBlob = new Blob(chunks, { type: "audio/webm" });
        sendAudioToBackend(audioBlob);
      };

      setMediaRecorder(recorder);
      setRecording(true);
      recorder.start();

      // ==========================================
      // 2. THE SILENCE DETECTOR
      // ==========================================
      audioContext.current = new AudioContext();
      const source = audioContext.current.createMediaStreamSource(stream);
      const analyzer = audioContext.current.createAnalyser();
      analyzer.fftSize = 512;
      source.connect(analyzer);

      const bufferLength = analyzer.frequencyBinCount;
      const dataArray = new Uint8Array(bufferLength);

      // This function runs 60 times a second to check the volume
      const checkSilence = () => {
        if (recorder.state !== "recording") return;

        analyzer.getByteFrequencyData(dataArray);

        // Calculate the average volume right now
        const currentVolume = dataArray.reduce((a, b) => a + b, 0) / bufferLength;

        // VOLUME THRESHOLD: 10 is usually a good baseline.
        // If it's above 10, someone is talking!
        if (currentVolume > 10) {
          // They are speaking! Cancel the silence timer.
          if (silenceTimer.current) {
            clearTimeout(silenceTimer.current);
            silenceTimer.current = null;
          }
        } else {
          // It is silent. If the timer isn't running yet, start it!
          if (!silenceTimer.current) {
            silenceTimer.current = setTimeout(() => {
              console.log("3 seconds of silence detected. Stopping!");
              recorder.stop(); // Automatically trigger Whisper!
            }, 3000); // 3000 milliseconds = 3 seconds
          }
        }

        // Loop this function to keep checking
        requestAnimationFrame(checkSilence);
      };

      checkSilence();

    } catch (error) {
      console.error("Error accessing microphone:", error);
    }
  };

  const playAudioResponse = async (textToSpeak: string) => {
  try {
    const response = await fetch("https://spanish-travel-companion.onrender.com/text-to-speech", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ text: textToSpeak, speed: audioSpeed }),
    });

    if (!response.ok) throw new Error("Failed to fetch audio");

    const audioBlob = await response.blob();
    const audioUrl = URL.createObjectURL(audioBlob);
    
    if (currentAudio) {
      currentAudio.pause();
    }
    
    currentAudio = new Audio(audioUrl);
    
    // ==========================================
    // THE CONTINUOUS LOOP HOOK
    // ==========================================
    currentAudio.onended = () => {
      console.log("Bot finished speaking. Turning microphone back on!");
      // Automatically trigger the microphone again!
      startRecording(); 
    };
    
    currentAudio.play();

  } catch (error) {
    console.error("Audio playback error:", error);
  }
};

const silenceTimer = useRef<NodeJS.Timeout | null>(null);
const idleTimer = useRef<NodeJS.Timeout | null>(null); // Add this new tracker!

// ... (keep the analyzer setup code above this the same) ...

      const bufferLength = analyzer.frequencyBinCount;
      const dataArray = new Uint8Array(bufferLength);

      // --- THE NEW LOGIC ---
      let hasSpoken = false; // Track if they ever made a sound

      const checkSilence = () => {
        if (recorder.state !== "recording") return;

        analyzer.getByteFrequencyData(dataArray);
        const currentVolume = dataArray.reduce((a, b) => a + b, 0) / bufferLength;

        if (currentVolume > 10) {
          // User is making noise! Mark that they have spoken.
          hasSpoken = true;

          // Clear BOTH timers because they are actively talking
          if (silenceTimer.current) {
            clearTimeout(silenceTimer.current);
            silenceTimer.current = null;
          }
          if (idleTimer.current) {
            clearTimeout(idleTimer.current);
            idleTimer.current = null;
          }
        } else {
          // It is silent. Which timer do we use?
          if (hasSpoken) {
            // They spoke previously, but paused. Wait 3 seconds to send message.
            if (!silenceTimer.current) {
              silenceTimer.current = setTimeout(() => {
                console.log("3 seconds of pause detected. Sending to bot!");
                recorder.stop(); // Triggers onstop -> sendAudioToBackend
              }, 3000); 
            }
          } else {
            // They haven't said a single word yet. Wait 10 seconds to shut down.
            if (!idleTimer.current) {
              idleTimer.current = setTimeout(() => {
                console.log("10 seconds of absolute silence. Shutting down.");
                
                // IMPORTANT: We delete the onstop function so it doesn't 
                // accidentally send 10 seconds of empty silence to OpenAI!
                recorder.onstop = null; 
                
                // Safely close the microphone loop
                stopRecording(); 

              }, 10000); // 10000 milliseconds = 10 seconds
            }
          }
        }

        // Loop this function to keep checking
        requestAnimationFrame(checkSilence);
      };

      checkSilence();

    } catch (error) {
// ... (keep the catch block the same) ...

const stopRecording = () => {
    if (mediaRecorder.current && mediaRecorder.current.state === "recording") {
      mediaRecorder.current.stop();
      setRecording(false);
    }

    if (silenceTimer.current) {
      clearTimeout(silenceTimer.current);
      silenceTimer.current = null;
    }

    // ADD THIS BLOCK:
    if (idleTimer.current) { 
      clearTimeout(idleTimer.current);
      idleTimer.current = null;
    }

    if (audioContext.current) {
      audioContext.current.close();
      audioContext.current = null;
    }
  };

  const startRecording = async () => {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
      const recorder = new MediaRecorder(stream);
      let chunks: BlobPart[] = [];

      recorder.ondataavailable = (e) => chunks.push(e.data);
      recorder.onstop = async () => {
        const audioBlob = new Blob(chunks, { type: 'audio/webm' });
        sendAudioToBackend(audioBlob);
      };

      setMediaRecorder(recorder);
      setRecording(true);
      recorder.start();

      // ==========================================
      // 2. THE SILENCE DETECTOR
      // ==========================================
      audioContext.current = new AudioContext();
      const source = audioContext.current.createMediaStreamSource(stream);
      const analyzer = audioContext.current.createAnalyser();
      analyzer.fftSize = 512;
      source.connect(analyzer);

      const bufferLength = analyzer.frequencyBinCount;
      const dataArray = new Uint8Array(bufferLength);

      let hasSpoken = false; // Track if they ever made a sound

      // We only need ONE checkSilence function!
      const checkSilence = () => {
        if (recorder.state !== "recording") return;

        analyzer.getByteFrequencyData(dataArray);
        const currentVolume = dataArray.reduce((a, b) => a + b, 0) / bufferLength;

        if (currentVolume > 10) {
          // User is making noise! Mark that they have spoken.
          hasSpoken = true;

          // Clear BOTH timers because they are actively talking
          if (silenceTimer.current) {
            clearTimeout(silenceTimer.current);
            silenceTimer.current = null;
          }
          if (idleTimer.current) {
            clearTimeout(idleTimer.current);
            idleTimer.current = null;
          }
        } else {
          // It is silent. Which timer do we use?
          if (hasSpoken) {
            // They spoke previously, but paused. Wait 3 seconds to send message.
            if (!silenceTimer.current) {
              silenceTimer.current = setTimeout(() => {
                console.log("3 seconds of pause detected. Sending to bot!");
                recorder.stop(); // Triggers onstop -> sendAudioToBackend
              }, 3000); 
            }
          } else {
            // They haven't said a single word yet. Wait 10 seconds to shut down.
            if (!idleTimer.current) {
              idleTimer.current = setTimeout(() => {
                console.log("10 seconds of absolute silence. Shutting down.");
                
                // IMPORTANT: We delete the onstop function so it doesn't 
                // accidentally send 10 seconds of empty silence to OpenAI!
                recorder.onstop = null; 
                
                // Safely close the microphone loop
                stopRecording(); 

              }, 10000); // 10000 milliseconds = 10 seconds
            }
          }
        }

        // Loop this function to keep checking
        requestAnimationFrame(checkSilence);
      };
  
      checkSilence();

    } catch (error) {
        console.error("Error accessing microphone:", error);
    }
  };
```

## 14
## April 26, 2026
## Gemini 3 Pro
## Used Gemini to incorporate memory into the bot system so that it can remember the last 10 messages or so.

```typescript
from typing import List, Dict
from pydantic import BaseModel

# 1. Update the blueprint to accept a list of messages
class ChatRequest(BaseModel):
    messages: List[Dict[str, str]]
    difficulty: str = "normal" # Keep any other variables you were using!

@app.post("/chat")
async def chat_endpoint(request: ChatRequest):
    try:
        # 2. Define the bot's "Personality" (System Prompt)
        # We prepend this to the history so the bot never forgets its job
        system_prompt = {
            "role": "system",
            "content": f"You are a helpful Spanish Travel Companion. Speak exclusively in Spanish at a {request.difficulty} level."
        }
        
        # 3. Combine the personality with the user's conversation history
        full_conversation = [system_prompt] + request.messages

        # 4. Send the ENTIRE script to OpenAI
        response = await client.chat.completions.create(
            model="gpt-4o-mini", # or whichever model you are using
            messages=full_conversation
        )
        
        bot_reply = response.choices[0].message.content
        return {"reply": bot_reply}

    except Exception as e:
        print(f"Chat API Error: {e}")
        raise HTTPException(status_code=500, detail=str(e))
// Define what a "Message" looks like for TypeScript
type Message = { role: "user" | "assistant", content: string };

// This array will hold the running conversation
const [chatHistory, setChatHistory] = useState<Message[]>([]);

const sendMessage = async (textOverride?: string) => {
  const messageToSend = textOverride || input;
  if (!messageToSend.trim()) return; 

  setLoading(true);

  // 1. Instantly update the UI and history with what the user just said
  const newUserMessage: Message = { role: "user", content: messageToSend };
  
  // LIGHTWEIGHT MEMORY TRICK: We use .slice(-6) to only keep the last 6 messages (3 interactions).
  // This prevents the context window from getting too large and eating all your API credits!
  const updatedHistory = [...chatHistory, newUserMessage].slice(-6); 
  
  setChatHistory(updatedHistory);
  setInput(""); // Clear the text box so they can type again

  try {
    const response = await fetch("https://spanish-travel-companion.onrender.com/chat", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      // 2. Send the ENTIRE updated history array to the backend!
      body: JSON.stringify({ messages: updatedHistory, difficulty: difficulty }), 
    });

    const data = await response.json();
    
    // 3. Add the bot's reply to the history array
    const newBotMessage: Message = { role: "assistant", content: data.reply };
    setChatHistory([...updatedHistory, newBotMessage]);
    
    // Keep this so your main UI still updates
    setReply(data.reply); 
      
    // 4. Trigger the Text-to-Speech!
    if (data.reply) {
       await playAudioResponse(data.reply);
    }
      
  } catch (error) {
    console.error("Error communicating with backend:", error);
  } finally {
    setLoading(false);
  }
};
```

## 15
## April 26, 2026
## Gemini 3 Pro
## Used Gemini to fix the UI of the website to make it a lot clean and modern.

```tsx
<main style={{ 
      minHeight: "100vh", 
      backgroundColor: "#f4f7f6", 
      display: "flex", 
      justifyContent: "center", 
      padding: "40px 20px",
      fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif"
    }}>
      
      {/* APP CARD CONTAINER */}
      <div style={{ 
        backgroundColor: "#ffffff", 
        maxWidth: "600px", 
        width: "100%", 
        borderRadius: "16px", 
        boxShadow: "0 10px 25px rgba(0, 0, 0, 0.05)", 
        padding: "40px 30px",
        display: "flex",
        flexDirection: "column",
        gap: "24px" // This creates perfectly even spacing between all sections
      }}>
        
        {/* HEADER */}
        <div style={{ textAlign: "center" }}>
          <h1 style={{ margin: "0 0 8px 0", color: "#111827", fontSize: "2rem" }}>Spanish Travel Companion</h1>
          <p style={{ margin: "0", color: "#6b7280", fontSize: "1rem" }}>Habla conmigo para practicar.</p>
        </div>

        {/* PRIMARY ACTION: VOICE RECORDING */}
        <div style={{ display: "flex", justifyContent: "center", padding: "10px 0" }}>
          {!recording ? (
            <button 
              onClick={startRecording} 
              style={{ 
                padding: "16px 32px", backgroundColor: "#10b981", color: "white", 
                border: "none", borderRadius: "50px", fontSize: "1.1rem", 
                fontWeight: "bold", cursor: "pointer", boxShadow: "0 4px 10px rgba(16, 185, 129, 0.3)",
                display: "flex", alignItems: "center", gap: "8px", transition: "transform 0.2s"
              }}
            >
              🎙️ Start Speaking
            </button>
          ) : (
            <button 
              onClick={stopRecording} 
              style={{ 
                padding: "16px 32px", backgroundColor: "#ef4444", color: "white", 
                border: "none", borderRadius: "50px", fontSize: "1.1rem", 
                fontWeight: "bold", cursor: "pointer", boxShadow: "0 4px 10px rgba(239, 68, 68, 0.3)",
                display: "flex", alignItems: "center", gap: "8px"
              }}
            >
              ⏹ Stop Recording
            </button>
          )}
        </div>

        {/* BOT RESPONSE AREA */}
        {reply && (
          <div style={{ 
            backgroundColor: "#f0fdf4", 
            borderLeft: "4px solid #10b981", 
            padding: "20px", 
            borderRadius: "0 8px 8px 0"
          }}>
            <h2 style={{ fontSize: "0.85rem", color: "#10b981", textTransform: "uppercase", letterSpacing: "1px", margin: "0 0 8px 0" }}>Companion Reply</h2>
            <p style={{ fontSize: "1.2rem", fontWeight: "500", color: "#111827", margin: "0", lineHeight: "1.5" }}>{reply}</p>
          </div>
        )}

        {/* TEXT INPUT BACKUP */}
        <div style={{ display: "flex", gap: "10px" }}>
          <input 
            type="text" 
            value={input}
            onChange={(e) => setInput(e.target.value)}
            placeholder="Or type your message here..."
            style={{ 
              flex: 1, padding: "14px", borderRadius: "8px", 
              border: "1px solid #e5e7eb", fontSize: "1rem", outline: "none",
              backgroundColor: "#f9fafb", color: "#111827"
            }}
          />
          <button 
            onClick={() => sendMessage()}
            disabled={loading}
            style={{ 
              padding: "0 24px", backgroundColor: loading ? "#9ca3af" : "#3b82f6", color: "white", 
              border: "none", borderRadius: "8px", fontWeight: "600",
              cursor: loading ? "not-allowed" : "pointer", transition: "background-color 0.2s"
            }}
          >
            {loading ? "..." : "Send"}
          </button>
        </div>

        {/* DIVIDER */}
        <hr style={{ border: "none", borderTop: "1px solid #e5e7eb", margin: "10px 0" }} />

        {/* SETTINGS GROUP */}
        <div style={{ display: "flex", flexDirection: "column", gap: "16px" }}>
          
          {/* Difficulty */}
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
            <span style={{ fontSize: "0.9rem", color: "#6b7280", fontWeight: "500" }}>Bot Level:</span>
            <div style={{ display: "flex", gap: "8px" }}>
              <button
                onClick={() => setDifficulty("Beginner")}
                style={{
                  backgroundColor: difficulty === "Beginner" ? "#ebf5ff" : "transparent",
                  color: difficulty === "Beginner" ? "#2563eb" : "#6b7280", 
                  border: difficulty === "Beginner" ? "1px solid #bfdbfe" : "1px solid #e5e7eb",
                  padding: "6px 12px", borderRadius: "6px", fontSize: "0.85rem", fontWeight: "600", cursor: "pointer"
                }}
              >
                Beginner
              </button>
              <button 
                onClick={() => setDifficulty("Intermediate")}
                style={{ 
                  backgroundColor: difficulty === "Intermediate" ? "#ebf5ff" : "transparent",
                  color: difficulty === "Intermediate" ? "#2563eb" : "#6b7280", 
                  border: difficulty === "Intermediate" ? "1px solid #bfdbfe" : "1px solid #e5e7eb",
                  padding: "6px 12px", borderRadius: "6px", fontSize: "0.85rem", fontWeight: "600", cursor: "pointer"
                }}
              >
                Intermediate
              </button>
            </div>
          </div>

          {/* Speed */}
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
            <span style={{ fontSize: "0.9rem", color: "#6b7280", fontWeight: "500" }}>Bot Speed:</span>
            <div style={{ display: "flex", gap: "8px" }}>
              {[
                { label: "Slow", val: 0.4 },
                { label: "Normal", val: 0.7 },
                { label: "Fast", val: 1 }
              ].map((speed) => (
                <button 
                  key={speed.val}
                  onClick={() => setAudioSpeed(speed.val)}
                  style={{ 
                    backgroundColor: audioSpeed === speed.val ? "#f3f4f6" : "transparent",
                    color: audioSpeed === speed.val ? "#111827" : "#6b7280",
                    border: audioSpeed === speed.val ? "1px solid #d1d5db" : "1px solid transparent",
                    padding: "6px 12px", borderRadius: "6px", fontSize: "0.85rem", fontWeight: "600", cursor: "pointer"
                  }}
                >
                  {speed.label}
                </button>
              ))}
            </div>
          </div>

        </div>

      </div>
    </main>
```